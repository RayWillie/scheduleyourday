var tasks = {
    timeblock: "",
    description: "",
};
// invoke current date to DOM
var currDate = moment().format("DD/MM/YYYY");
$("#currentDay").text( "Today's date: " + currDate );

function addTasks() {
    var taskAdd = "";

    $(".tasks").on( "click", );
    
    console.log();
}
function saveTask() {

}
addTasks();